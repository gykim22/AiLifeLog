spring:
  application:
    name: Ai-Lifelog
  profiles:
    include: db,ai
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
      format_sql: true
      dialect: org.hibernate.dialect.PostgreSQLDialect

